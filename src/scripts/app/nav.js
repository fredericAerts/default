var defaultApp = defaultApp || {};

defaultApp.nav = (($, window, undefined) => {

    var init, testFunction;


    init = () => {
        
    };


    testFunction = (test) => {
        return () => {
            
        };
    };

    return {
        init: init
    };

}(jQuery, window));

